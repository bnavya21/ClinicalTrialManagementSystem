import { CurrentEmployeeUserName } from './current-employee-user-name';

describe('CurrentEmployeeUserName', () => {
  it('should create an instance', () => {
    expect(new CurrentEmployeeUserName()).toBeTruthy();
  });
});
